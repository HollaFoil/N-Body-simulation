using N_Body_simulation.Render;
using System;
using static OpenGL.GL;

namespace N_Body_simulation // Note: actual namespace depends on the project name.
{
    internal class Program
    {
        static void Main()
        {
            Window.Init();
            while (true)
            {

            }
        }
    }
}